# Use DecapBridge classic auth (required)
backend:
  name: git-gateway
  repo: baalebos-cloud/townscribe
  branch: main
  identity_url: https://auth.decapbridge.com/sites/95458850-880f-4740-8e26-715c035f3ed4
  gateway_url: https://gateway.decapbridge.com

  # See who did what (optional)
  commit_messages:
    create: Create {{collection}} “{{slug}}” - {{author-name}} <{{author-login}}> via DecapBridge
    update: Update {{collection}} “{{slug}}” - {{author-name}} <{{author-login}}> via DecapBridge
    delete: Delete {{collection}} “{{slug}}” - {{author-name}} <{{author-login}}> via DecapBridge
    uploadMedia: Upload “{{path}}” - {{author-name}} <{{author-login}}> via DecapBridge
    deleteMedia: Delete “{{path}}” - {{author-name}} <{{author-login}}> via DecapBridge
    openAuthoring: Message {{message}} - {{author-name}} <{{author-login}}> via DecapBridge

# Better Decap + Bridge logo (optional)
logo_url: https://decapbridge.com/decapcms-with-bridge.svg

# Add site links in Decap CMS (optional)
site_url: https://townscribe.org

# Activates "Editorial Workflow" for a neat Drafts vs Published kanban board layout
publish_mode: editorial_workflow

media_folder: "public" 
public_folder: "/"     

collections:
  - name: "blog"
    label: "News Articles"
    folder: "src/content/blog" # Direct injection path into Astro content directory
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Description", name: "description", widget: "string" }
      - { label: "Publish Date", name: "pubDate", widget: "datetime" }
      
      # --- WRITER & AUTHOR ATTRIBUTION ---
      - { label: "Author Name", name: "author", widget: "string", default: "TownScribe Staff", hint: "Type your name as it should appear on the article" }
      - { label: "Author Role", name: "authorRole", widget: "select", options: ["Admin", "Editor", "Writer", "Contributor"], default: "Writer" }

      # --- AUTOMATED CATEGORIES LINKING BAR ---
      - label: "Category"
        name: "category"
        widget: "select"
        default: "world"
        options:
          - { label: "Politics", value: "politics" }
          - { label: "Business", value: "business" }
          - { label: "Entertainment", value: "entertainment" }
          - { label: "Technology", value: "technology" }
          - { label: "Health", value: "health" }
          - { label: "Sport", value: "sport" }
          - { label: "World", value: "world" }
          - { label: "Job", value: "job" }

      - { label: "Hero Image", name: "heroImage", widget: "image", required: false, default: "/townscribe-img.jpg" }
      - { label: "Body", name: "body", widget: "markdown" }

      # --- AUTOMATED YOUTUBE REDIRECT TIMELINE ENGINE ---
      - { label: "YouTube Video URL", name: "youtubeUrl", widget: "string", required: false, hint: "Paste the full YouTube link here (e.g., https://www.youtube.com/watch?v=dQw4w9WgXcQ)" }

      # --- SEO FIELDS FOR WRITERS ---
      - { label: "SEO Meta Title", name: "seoTitle", widget: "string", required: false, hint: "Optional: Custom title for Google search results" }
      - { label: "SEO Meta Description", name: "seoDescription", widget: "text", required: false, hint: "Optional: A brief summary under 160 characters for search engines" }
